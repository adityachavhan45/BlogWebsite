{
  "name": "solution-blogs-server",
  "version": "1.0.0",
  "description": "Backend server for blog application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "clean": "rimraf node_modules package-lock.json && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "rimraf": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
